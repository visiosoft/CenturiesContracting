[build]
  base = "client"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--include=dev"

# Proxy API calls to Express backend
[[redirects]]
  from = "/api/*"
  to = "https://centuries.mypaperlessoffice.org:5020/api/:splat"
  status = 200
  force = true

# SPA fallback — must be last
# Note: /projects/* is served directly from client/public/projects/ by Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
